{
  "name": "git/clean",
  "description": "Validate git working tree has no uncommitted changes",
  "nodes": {
    "diff": { "type": "command", "description": "git tree clean", "run": ["git", "diff", "--quiet", "HEAD"] },
    "status": { "type": "command", "description": "git dirty files", "run": ["git", "status", "--porcelain"] }
  }
}

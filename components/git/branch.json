{
  "name": "git/branch",
  "description": "Capture current git branch",
  "nodes": {
    "branch": { "type": "command", "description": "git current branch", "run": ["git", "rev-parse", "--abbrev-ref", "HEAD"] }
  }
}

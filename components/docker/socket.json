{
  "name": "docker/socket",
  "description": "Validate Docker socket exists and is accessible",
  "nodes": {
    "socket": { "type": "file", "path": "/var/run/docker.sock", "expect": "socket" }
  }
}

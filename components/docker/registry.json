{
  "name": "docker/registry",
  "description": "Validate Docker registry authentication",
  "nodes": {
    "docker/daemon": { "type": "component" },
    "auth": { "type": "command", "description": "docker registry auth", "run": ["docker", "login", "--get-login", "docker.io"] }
  }
}

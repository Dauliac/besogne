{
  "name": "docker/daemon",
  "description": "Validate Docker daemon is running via local socket",
  "nodes": {
    "docker/docker": { "type": "plugin" },
    "info": { "type": "command", "description": "docker daemon info", "run": ["docker", "info", "--format", "{{json .}}"] }
  }
}

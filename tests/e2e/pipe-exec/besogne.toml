name = "pipe-exec"
description = "Test pipe exec form — echo | tr"

[nodes.echo-bin]
type = "binary"
name = "echo"

[nodes.tr-bin]
type = "binary"
name = "tr"

[nodes.pipe-test]
type = "command"
phase = "exec"
run = { pipe = [["echo", "hello world"], ["tr", "a-z", "A-Z"]] }

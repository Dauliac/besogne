name = "failure-stops"
description = "Failing command prevents dependents from running"

type = "component"

[nodes.fail]
type = "command"
phase = "exec"
run = ["sh", "-c", "echo 'this fails'; exit 1"]

[nodes.should-skip]
type = "command"
phase = "exec"
run = ["sh", "-c", "echo 'THIS_MUST_NOT_RUN' >> leaked.txt"]
parents = ["fail"]

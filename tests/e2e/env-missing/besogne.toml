name = "env-missing-fail"
description = "Fail when required env var is missing (exit 2, no commands run)"

[inputs.BESOGNE_E2E_REQUIRED_MISSING_VAR]
type = "env"

[inputs.should-not-run]
type = "command"
phase = "exec"
run = ["echo", "THIS_SHOULD_NOT_APPEAR"]

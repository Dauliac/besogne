[env]
PROJECT_NAME = "myapp"
PROJECT_VERSION = "1.0.0"

[tasks.compile]
run = "besogne build -i manifest.json -o ./task"

[tasks.run]
run = "./task"
depends = ["compile"]

[tasks.clean]
run = "rm -rf ./task dist"
